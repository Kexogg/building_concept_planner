const Header = () => {
    return (
        <header className={"w-full h-16 flex border-b-2 items-center justify-center"}>
            <div className={"container mx-5 font-bold"}>Building concept planner</div>
        </header>
    );
};

export default Header;